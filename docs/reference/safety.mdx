---
title: "Safety model"
description: "Review the local process trust boundary, plan-first mutations, rollback expectations, and secret-handling rules."
---

## Process authority

The local MCP server inherits the permissions of its process:

- Docker daemon access can control containers and may imply broad host privileges.
- SSH profiles execute with the selected remote identity.
- YDB operations use the endpoint and credentials available to the process.
- Configuration, password, dump, restore, and auth files follow filesystem permissions.

Run the server with the least-privileged operating-system account and target
credentials that can complete the task.

## Plan-first mutations

Mutating tools return planned commands, risk, rollback, verification, and a
one-time process-bound token without executing. After human review, read the
token from the plan response's `confirmation.token`, then repeat the same
request with `confirm: true` and that value in the `confirmationToken` request
argument.

```json
{
  "confirm": true,
  "confirmationToken": "<value from confirmation.token>"
}
```

The token is bound to the exact tool, target, config source, execution inputs,
current plan, risk, rollback, and verification. Execution inputs include DDL,
SQL parameters, secret-bearing command input, and private content fingerprints
for configured auth files or the selected standalone restore dump; contents and
fingerprints are never returned. The token is valid only until use or MCP
restart and is consumed before the first side effect. A missing, changed,
replayed, or pre-restart token executes nothing and returns a fresh plan/token.
A valid submitted token is also retired when a refreshed prerequisite or image
check makes the requested mutation a no-op, so it cannot become executable
again if state changes later. Content placeholders are resolved only in their
declared internal command fields; marker-shaped user arguments are not scanned
or rewritten globally.

For accepted auth config, password-rotation backup inputs, dynamic-node token,
and standalone restore operations, the server first creates a private snapshot
and verifies it against the authorized fingerprint. Docker and YDB consume only
that snapshot; the canonical payload path is not reopened. Credential files
used only to authenticate remain freshness guards. Dynamic tokens are copied
into stopped containers before start, and restore mounts its snapshot read-only.
Restore rejects symlinks and special filesystem entries before mutation.
Snapshot contents, digests, and temporary paths are omitted from responses and
cleaned after success, failure, or abort. Restore tries copy-on-write or reflink
cloning first, but its portable fallback can temporarily require free space
equal to the full dump size. Composite storage reduction and version upgrade
make the same kind of private verified copy immediately after generating their
dump and before teardown, then restore only from that copy. Their teardown
container set is frozen from the reviewed inventory instead of expanding to
late arrivals. Version upgrade never rewrites the profile config automatically.
After independently verifying container images and restored data, update
`profiles.<name>.image` manually; ordinary files, symlinks, and concurrent edits
remain untouched by the tool.

Treat the token as an ephemeral capability: do not log or persist it. It does
not prove that a human approved the plan; the MCP host/client must enforce that
approval. This gate does not replace operating-system sandboxing,
authentication, authorization, branch protection, backups, or least-privilege
credentials.

## Destructive workflows

Inspect exact targets before cleanup, destroy, storage reduction, restore, or
version upgrade. Prefer dump, fresh rebuild, restore, and verification over an
unrehearsed in-place data migration. Keep a recoverable rollback artifact until
tenant metadata, table reads, auth, node state, and storage placement pass.

## Secrets and private data

Keep these values outside the repository and out of prompts:

- passwords, tokens, and password-file contents;
- SSH private keys and private host details;
- local config paths that identify private infrastructure;
- dump, restore, and backup paths from real environments.

Use placeholders in public examples and report vulnerabilities through
[GitHub Private Vulnerability Reporting](https://github.com/astandrik/local-ydb-toolkit/security/advisories/new).
