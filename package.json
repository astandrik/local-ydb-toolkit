{
  "name": "local-ydb-toolkit",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @local-ydb-toolkit/core && npm run build -w @astandrik/local-ydb-mcp && npm run docs:check",
    "docs:generate": "npm run build -w @local-ydb-toolkit/core && npm run build -w @astandrik/local-ydb-mcp && node scripts/ci/generate-mcp-tools-docs.mjs --write",
    "docs:check": "node scripts/ci/generate-mcp-tools-docs.mjs --check",
    "test": "vitest run",
    "posttest": "node --test scripts/ci/*.test.mjs",
    "typecheck": "npm run typecheck --workspaces"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3",
    "vitest": "^4.1.5"
  }
}
