# local-ydb

Use this skill for local-ydb setup, diagnostics, maintenance, and MCP workflows.

## Scripts

Place task-specific helper scripts in `scripts/`.

## References

Place local-ydb notes, command references, and operational runbooks in `references/`.

## Assets

Place static assets used by the skill in `assets/`.
