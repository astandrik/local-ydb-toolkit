{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "should_use_local_ydb_skill": {
      "type": "boolean"
    },
    "task_type": {
      "type": "string"
    },
    "tool_sequence": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "MCP tool name, optionally followed by space-separated key=value argument summaries. Use placeholders such as <generated-script> for prior call results; do not use a JSON argument object."
      }
    },
    "safety_gates": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "would_execute_confirmed_mutation": {
      "type": "boolean"
    },
    "answer": {
      "type": "string"
    }
  },
  "required": [
    "should_use_local_ydb_skill",
    "task_type",
    "tool_sequence",
    "safety_gates",
    "would_execute_confirmed_mutation",
    "answer"
  ]
}
